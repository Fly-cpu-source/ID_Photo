<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" >
  <meta name="viewport" content="width=device-width, initial-scale=1.0" >
  <title>VISAGE — 智能证件照</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" >
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin >
  <link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,200;0,9..144,300;0,9..144,400;1,9..144,200;1,9..144,300;1,9..144,400&family=Syne:wght@400;500;600;700&display=swap" rel="stylesheet" >
  <style>
    :root {
      --bg:         #08080C;
      --surface:    #0F0F14;
      --surface2:   #151519;
      --surface3:   #1C1C22;
      --border:     #202028;
      --border2:    #2E2E3A;
      --gold:       #C4A35A;
      --gold-light: #D4B870;
      --gold-dim:   #7A6535;
      --gold-glow:  rgba(196,163,90,0.10);
      --gold-glow2: rgba(196,163,90,0.06);
      --text:       #ECE7DE;
      --text2:      #B8B0A4;
      --muted:      #5E5E6E;
      --error:      #D95B5B;
      --success:    #5BAD7A;
    }

    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    html { scroll-behavior: smooth; font-size: 16px; }

    body {
      font-family: 'Syne', sans-serif;
      background: var(--bg);
      color: var(--text);
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* Grain overlay */
    body::after {
      content: '';
      position: fixed; inset: 0;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 512 512' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.75' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='1'/%3E%3C/svg%3E");
      opacity: 0.022;
      pointer-events: none;
      z-index: 9000;
    }

    /* ─── NAVIGATION ─── */
    nav {
      position: fixed; top: 0; left: 0; right: 0; z-index: 500;
      display: flex; align-items: center; justify-content: space-between;
      padding: 0 56px; height: 68px;
      border-bottom: 1px solid transparent;
      transition: border-color 0.4s, background 0.4s, backdrop-filter 0.4s;
    }
    nav.scrolled {
      border-color: var(--border);
      background: rgba(8,8,12,0.88);
      backdrop-filter: blur(24px);
    }
    .nav-logo {
      font-family: 'Fraunces', serif;
      font-size: 22px; font-weight: 300;
      letter-spacing: 0.22em; color: var(--gold); text-transform: uppercase;
      text-decoration: none;
    }
    .nav-links {
      display: flex; align-items: center; gap: 40px;
      list-style: none;
    }
    .nav-links a {
      font-size: 12px; font-weight: 500; letter-spacing: 0.14em;
      text-transform: uppercase; color: var(--muted);
      text-decoration: none; transition: color 0.2s;
    }
    .nav-links a:hover { color: var(--text); }
    .nav-cta {
      font-size: 12px; font-weight: 600; letter-spacing: 0.16em;
      text-transform: uppercase; color: var(--bg);
      background: var(--gold);
      padding: 10px 22px; border-radius: 6px;
      text-decoration: none;
      transition: background 0.2s, transform 0.15s;
    }
    .nav-cta:hover { background: var(--gold-light); transform: translateY(-1px); }

    /* ─── HERO ─── */
    .hero {
      min-height: 100vh; display: flex; flex-direction: column;
      align-items: center;
      padding: 80px 48px 40px; text-align: center;
      position: relative; overflow: hidden;
    }
    .hero-content {
      flex: 1; display: flex; flex-direction: column;
      align-items: center; justify-content: center;
      width: 100%;
    }

    /* Animated spotlight */
    .hero-bg {
      position: absolute; inset: 0; pointer-events: none;
      overflow: hidden;
    }
    .hero-spot {
      position: absolute; top: 50%; left: 50%;
      transform: translate(-50%, -50%);
      width: 900px; height: 900px;
      background: radial-gradient(ellipse, rgba(196,163,90,0.07) 0%, transparent 68%);
      animation: pulse 6s ease-in-out infinite;
    }
    .hero-spot-2 {
      position: absolute; top: 30%; left: 60%;
      width: 500px; height: 500px;
      background: radial-gradient(ellipse, rgba(100,80,200,0.04) 0%, transparent 70%);
      animation: pulse 8s 2s ease-in-out infinite;
    }

    /* Dot grid */
    .hero-grid {
      position: absolute; inset: 0;
      background-image: radial-gradient(circle, rgba(200,168,110,0.12) 1px, transparent 1px);
      background-size: 48px 48px;
      mask-image: radial-gradient(ellipse 80% 60% at center, black 0%, transparent 100%);
      opacity: 0.3;
    }

    .hero-eyebrow {
      display: inline-flex; align-items: center; gap: 10px;
      font-size: 11px; font-weight: 500; letter-spacing: 0.3em; text-transform: uppercase;
      color: var(--gold);
      margin-bottom: 36px;
      animation: fadeUp 0.9s ease both;
    }
    .hero-eyebrow::before, .hero-eyebrow::after {
      content: ''; display: block;
      width: 32px; height: 1px; background: var(--gold-dim);
    }

    .hero h1 {
      font-family: 'Fraunces', serif;
      font-size: clamp(56px, 9vw, 108px);
      font-weight: 200; line-height: 1.0;
      letter-spacing: -0.01em; color: var(--text);
      margin-bottom: 28px;
      animation: fadeUp 0.9s 0.12s ease both;
    }
    .hero h1 em {
      font-style: italic; color: var(--gold);
      font-weight: 300;
    }
    .hero h1 .line2 {
      display: block;
      color: var(--text2);
    }

    .hero-desc {
      font-size: 17px; font-weight: 400; color: var(--text2);
      letter-spacing: 0.02em; max-width: 480px;
      line-height: 1.75; margin: 0 auto 48px;
      animation: fadeUp 0.9s 0.22s ease both;
    }

    .hero-actions {
      display: flex; align-items: center; gap: 16px;
      animation: fadeUp 0.9s 0.32s ease both;
    }
    .btn-hero-primary {
      display: inline-flex; align-items: center; gap: 10px;
      padding: 16px 36px; border-radius: 8px;
      background: var(--gold);
      color: var(--bg); text-decoration: none;
      font-size: 13px; font-weight: 700; letter-spacing: 0.16em; text-transform: uppercase;
      transition: background 0.2s, transform 0.15s, box-shadow 0.2s;
    }
    .btn-hero-primary:hover { background: var(--gold-light); transform: translateY(-2px); box-shadow: 0 12px 32px rgba(196,163,90,0.3); }
    .btn-hero-primary svg { width: 16px; height: 16px; fill: none; stroke: currentColor; stroke-width: 2; }
    .btn-hero-ghost {
      display: inline-flex; align-items: center; gap: 8px;
      padding: 16px 28px; border-radius: 8px;
      border: 1px solid var(--border2); color: var(--text2);
      text-decoration: none;
      font-size: 13px; font-weight: 500; letter-spacing: 0.12em; text-transform: uppercase;
      transition: border-color 0.2s, color 0.2s;
    }
    .btn-hero-ghost:hover { border-color: var(--gold-dim); color: var(--gold); }

    .hero-scroll {
      display: flex; flex-direction: column; align-items: center; gap: 8px;
      margin-top: 56px;
      padding-bottom: 8px;
      animation: fadeUp 0.9s 0.5s ease both;
    }
    .scroll-line {
      width: 1px; height: 48px; background: linear-gradient(to bottom, var(--gold-dim), transparent);
      animation: scrollPulse 2.5s ease-in-out infinite;
    }
    .scroll-label { font-size: 10px; letter-spacing: 0.3em; text-transform: uppercase; color: var(--muted); }

    /* ─── SECTION COMMON ─── */
    .section-eyebrow {
      display: inline-flex; align-items: center; gap: 12px;
      font-size: 11px; font-weight: 600; letter-spacing: 0.3em; text-transform: uppercase;
      color: var(--gold); margin-bottom: 20px;
    }
    .section-eyebrow::before {
      content: ''; display: block;
      width: 24px; height: 1px; background: var(--gold-dim);
    }
    .section-title {
      font-family: 'Fraunces', serif;
      font-size: clamp(36px, 5vw, 56px); font-weight: 200;
      line-height: 1.12; color: var(--text); margin-bottom: 16px;
    }
    .section-title em { font-style: italic; color: var(--gold); }
    .section-sub {
      font-size: 16px; font-weight: 400; color: var(--text2); line-height: 1.7;
      max-width: 520px;
    }

    /* ─── FEATURES ─── */
    .features {
      padding: 120px 56px;
      max-width: 1200px; margin: 0 auto;
    }
    .features-header { margin-bottom: 80px; }
    .features-grid {
      display: grid; grid-template-columns: repeat(2, 1fr); gap: 2px;
    }
    .feature-card {
      padding: 48px 44px;
      background: var(--surface);
      border: 1px solid var(--border);
      position: relative; overflow: hidden;
      transition: border-color 0.3s, background 0.3s;
    }
    .feature-card:nth-child(1) { border-radius: 16px 0 0 0; }
    .feature-card:nth-child(2) { border-radius: 0 16px 0 0; }
    .feature-card:nth-child(3) { border-radius: 0 0 0 16px; }
    .feature-card:nth-child(4) { border-radius: 0 0 16px 0; }
    .feature-card::before {
      content: ''; position: absolute; top: 0; left: 0; right: 0; height: 1px;
      background: linear-gradient(to right, transparent, var(--gold-dim), transparent);
      opacity: 0; transition: opacity 0.3s;
    }
    .feature-card:hover { border-color: var(--border2); background: var(--surface2); }
    .feature-card:hover::before { opacity: 1; }

    .feature-num {
      font-family: 'Fraunces', serif;
      font-size: 72px; font-weight: 200; font-style: italic;
      color: var(--border2); line-height: 1;
      margin-bottom: 24px; letter-spacing: -0.02em;
      transition: color 0.3s;
    }
    .feature-card:hover .feature-num { color: var(--gold-dim); }
    .feature-icon {
      width: 48px; height: 48px; border: 1px solid var(--border2); border-radius: 12px;
      display: flex; align-items: center; justify-content: center;
      margin-bottom: 20px; background: var(--surface2);
      transition: border-color 0.3s, background 0.3s;
    }
    .feature-card:hover .feature-icon { border-color: var(--gold-dim); background: var(--gold-glow); }
    .feature-icon svg { width: 22px; height: 22px; stroke: var(--muted); fill: none; stroke-width: 1.5; transition: stroke 0.3s; }
    .feature-card:hover .feature-icon svg { stroke: var(--gold); }
    .feature-name {
      font-size: 18px; font-weight: 600; color: var(--text);
      letter-spacing: 0.01em; margin-bottom: 12px;
    }
    .feature-desc { font-size: 15px; font-weight: 400; color: var(--text2); line-height: 1.7; }

    /* ─── HOW IT WORKS ─── */
    .how {
      padding: 120px 56px;
      max-width: 1200px; margin: 0 auto;
    }
    .how-header { margin-bottom: 72px; }
    .how-steps {
      display: grid; grid-template-columns: repeat(3, 1fr);
      position: relative;
    }
    .how-steps::before {
      content: '';
      position: absolute; top: 32px; left: calc(16.67% + 32px); right: calc(16.67% + 32px);
      height: 1px;
      background: linear-gradient(to right, var(--gold-dim), var(--border2), var(--gold-dim));
    }
    .how-step {
      display: flex; flex-direction: column; align-items: center; text-align: center;
      padding: 0 32px;
    }
    .how-step-num {
      width: 64px; height: 64px; border-radius: 50%;
      border: 1px solid var(--gold-dim); background: var(--surface2);
      display: flex; align-items: center; justify-content: center;
      font-family: 'Fraunces', serif; font-size: 22px; font-weight: 300; font-style: italic;
      color: var(--gold); margin-bottom: 28px; position: relative; z-index: 1;
      transition: background 0.3s, border-color 0.3s;
    }
    .how-step:hover .how-step-num { background: var(--gold-glow); border-color: var(--gold); }
    .how-step-title {
      font-size: 17px; font-weight: 600; color: var(--text);
      margin-bottom: 12px; letter-spacing: 0.01em;
    }
    .how-step-desc {
      font-size: 14px; font-weight: 400; color: var(--text2); line-height: 1.7;
    }
    .how-step-tag {
      display: inline-block; margin-top: 16px;
      font-size: 10px; font-weight: 600; letter-spacing: 0.22em; text-transform: uppercase;
      color: var(--gold-dim); border: 1px solid var(--border2);
      padding: 4px 10px; border-radius: 100px;
    }

    /* ─── PRICING ─── */
    .pricing {
      padding: 120px 56px;
      max-width: 1200px; margin: 0 auto;
    }
    .pricing-header { margin-bottom: 64px; }
    .pricing-grid {
      display: grid; grid-template-columns: repeat(2, 1fr); gap: 24px;
      max-width: 900px;
    }
    .pricing-card {
      padding: 44px; border-radius: 16px;
      border: 1px solid var(--border);
      background: var(--surface);
      position: relative; overflow: hidden;
    }
    .pricing-card.featured {
      border-color: var(--gold-dim);
      background: linear-gradient(145deg, var(--surface2) 0%, var(--surface) 100%);
    }
    .pricing-card.featured::before {
      content: ''; position: absolute; inset: 0;
      background: radial-gradient(ellipse at top left, rgba(196,163,90,0.06) 0%, transparent 60%);
      pointer-events: none;
    }
    .pricing-badge {
      display: inline-flex; align-items: center;
      font-size: 11px; font-weight: 600; letter-spacing: 0.2em; text-transform: uppercase;
      color: var(--bg); background: var(--gold);
      padding: 5px 12px; border-radius: 100px;
      margin-bottom: 24px;
    }
    .pricing-badge.soon {
      color: var(--muted); background: var(--surface3);
    }
    .pricing-plan {
      font-family: 'Fraunces', serif;
      font-size: 32px; font-weight: 300; color: var(--text);
      margin-bottom: 8px;
    }
    .pricing-price {
      font-size: 48px; font-weight: 700; color: var(--gold);
      line-height: 1; margin-bottom: 6px;
    }
    .pricing-card:not(.featured) .pricing-price { color: var(--muted); }
    .pricing-period { font-size: 14px; color: var(--muted); margin-bottom: 32px; }
    .pricing-divider { height: 1px; background: var(--border); margin-bottom: 28px; }
    .pricing-features { list-style: none; display: flex; flex-direction: column; gap: 14px; }
    .pricing-features li {
      display: flex; align-items: center; gap: 12px;
      font-size: 15px; color: var(--text2);
    }
    .pricing-features li svg {
      width: 16px; height: 16px; flex-shrink: 0;
      stroke: var(--success); fill: none; stroke-width: 2.5;
    }
    .pricing-features li.dimmed svg { stroke: var(--border2); }
    .pricing-features li.dimmed { color: var(--muted); }
    .pricing-cta {
      display: block; margin-top: 36px; padding: 14px;
      border-radius: 8px; text-align: center; text-decoration: none;
      font-size: 13px; font-weight: 600; letter-spacing: 0.14em; text-transform: uppercase;
      transition: background 0.2s, color 0.2s, transform 0.15s;
      cursor: pointer;
    }
    .pricing-cta.primary {
      background: var(--gold); color: var(--bg);
    }
    .pricing-cta.primary:hover { background: var(--gold-light); transform: translateY(-1px); }
    .pricing-cta.ghost {
      border: 1px solid var(--border2); color: var(--muted);
      cursor: not-allowed;
    }

    /* ─── STUDIO ─── */
    .studio-section {
      padding: 80px 0 140px;
      position: relative;
    }
    .studio-header {
      text-align: center; padding: 0 56px; margin-bottom: 56px;
    }
    .studio-wrap {
      max-width: 1160px; margin: 0 auto; padding: 0 40px;
    }

    .workspace {
      display: grid; grid-template-columns: 1fr 420px;
      gap: 2px;
      border-radius: 18px; overflow: hidden;
      box-shadow: 0 40px 80px rgba(0,0,0,0.5);
    }

    /* Upload panel */
    .upload-panel {
      background: var(--surface);
      border: 1px solid var(--border);
      border-right: none; border-radius: 18px 0 0 18px;
      padding: 44px; display: flex; flex-direction: column; gap: 28px;
    }
    .panel-label {
      font-size: 11px; letter-spacing: 0.28em; text-transform: uppercase;
      color: var(--gold-dim); display: flex; align-items: center; gap: 12px;
    }
    .panel-label::after { content: ''; flex: 1; height: 1px; background: var(--border); }

    .upload-zone {
      position: relative; border: 1px solid var(--border2); border-radius: 12px;
      min-height: 400px; flex: 1;
      display: flex; align-items: center; justify-content: center;
      cursor: pointer; overflow: hidden; transition: border-color 0.3s;
      background: var(--surface2);
    }
    .upload-zone::before {
      content: ''; position: absolute; inset: 0;
      background: radial-gradient(ellipse at 50% 0%, rgba(196,163,90,0.05) 0%, transparent 65%);
      opacity: 0; transition: opacity 0.3s;
    }
    .upload-zone:hover { border-color: var(--gold-dim); }
    .upload-zone:hover::before { opacity: 1; }
    .upload-zone.drag-over { border-color: var(--gold); border-style: dashed; }
    .upload-zone.drag-over::before { opacity: 1; }

    .upload-hint {
      text-align: center; pointer-events: none;
      display: flex; flex-direction: column; align-items: center; gap: 18px;
    }
    .upload-icon-wrap {
      width: 72px; height: 72px; border: 1px solid var(--border2); border-radius: 50%;
      display: flex; align-items: center; justify-content: center;
      background: var(--surface3); transition: all 0.3s;
    }
    .upload-zone:hover .upload-icon-wrap { border-color: var(--gold-dim); background: var(--gold-glow); }
    .upload-icon-wrap svg { width: 28px; height: 28px; stroke: var(--muted); fill: none; stroke-width: 1.5; transition: stroke 0.3s; }
    .upload-zone:hover .upload-icon-wrap svg { stroke: var(--gold); }
    .upload-hint-title { font-size: 16px; font-weight: 500; color: var(--text); letter-spacing: 0.01em; }
    .upload-hint-sub { font-size: 13px; color: var(--muted); font-weight: 400; letter-spacing: 0.03em; }

    .preview-img { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: contain; padding: 20px; }
    .preview-replace {
      position: absolute; bottom: 14px; right: 14px;
      background: rgba(8,8,12,0.8); backdrop-filter: blur(8px);
      border: 1px solid var(--border2); border-radius: 7px;
      padding: 8px 14px; font-size: 12px; letter-spacing: 0.1em; color: var(--muted);
      cursor: pointer; transition: color 0.2s, border-color 0.2s;
    }
    .preview-replace:hover { color: var(--gold); border-color: var(--gold-dim); }

    /* Settings panel */
    .settings-panel {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 0 18px 18px 0;
      padding: 44px 32px;
      display: flex; flex-direction: column; gap: 0;
      overflow: hidden;
    }
    .setting-block {
      padding: 20px 0;
      border-bottom: 1px solid var(--border);
    }
    .setting-block:first-child { padding-top: 0; }
    .setting-block:last-child { border-bottom: none; padding-bottom: 0; }
    .setting-block.grow {
      flex: 1; overflow: hidden; display: flex; flex-direction: column; gap: 12px;
      padding-bottom: 0;
    }
    .setting-title {
      font-size: 11px; letter-spacing: 0.28em; text-transform: uppercase;
      color: var(--gold-dim); margin-bottom: 14px;
    }

    /* Color swatches */
    .color-swatches { display: grid; grid-template-columns: repeat(4, 1fr); gap: 8px; }
    .swatch {
      aspect-ratio: 1; border-radius: 9px; cursor: pointer;
      border: 2px solid transparent; position: relative;
      transition: transform 0.15s, border-color 0.15s; overflow: hidden;
    }
    .swatch:hover { transform: scale(1.07); }
    .swatch.active { border-color: var(--gold) !important; box-shadow: 0 0 0 3px var(--gold-glow); }
    .swatch.active::after {
      content: '✓'; position: absolute; inset: 0;
      display: flex; align-items: center; justify-content: center;
      font-size: 14px; color: rgba(0,0,0,0.4); font-weight: 700;
    }
    .swatch-white.active::after { color: rgba(0,0,0,0.3); }
    .swatch-custom {
      background: var(--surface3); border: 1.5px dashed var(--border2) !important;
      display: flex; align-items: center; justify-content: center; position: relative;
    }
    .swatch-custom input[type="color"] { position: absolute; inset: 0; opacity: 0; cursor: pointer; width: 100%; height: 100%; }
    .swatch-custom-icon { font-size: 20px; color: var(--muted); pointer-events: none; line-height: 1; }

    /* Model list */
    .model-list { display: flex; flex-direction: column; gap: 6px; }
    .model-option {
      display: flex; align-items: center; justify-content: space-between;
      padding: 11px 14px; border: 1px solid var(--border);
      border-radius: 8px; cursor: pointer;
      transition: border-color 0.2s, background 0.2s;
      background: var(--surface2);
    }
    .model-option:hover { border-color: var(--border2); }
    .model-option.active { border-color: var(--gold-dim); background: var(--gold-glow); }
    .model-name { font-size: 14px; font-weight: 500; color: var(--text); }
    .model-desc { font-size: 11px; color: var(--muted); letter-spacing: 0.02em; }

    /* Size list */
    .size-list {
      display: flex; flex-direction: column; gap: 4px;
      flex: 1; overflow-y: auto; padding-right: 4px;
    }
    .size-list::-webkit-scrollbar { width: 3px; }
    .size-list::-webkit-scrollbar-track { background: transparent; }
    .size-list::-webkit-scrollbar-thumb { background: var(--border2); border-radius: 3px; }

    .size-category {
      font-size: 10px; letter-spacing: 0.24em; text-transform: uppercase;
      color: var(--muted); padding: 12px 2px 5px;
    }
    .size-category:first-child { padding-top: 0; }
    .size-option {
      display: flex; align-items: center; justify-content: space-between;
      padding: 11px 13px; border: 1px solid var(--border);
      border-radius: 8px; cursor: pointer;
      transition: border-color 0.2s, background 0.2s;
      background: var(--surface2);
    }
    .size-option:hover { border-color: var(--border2); }
    .size-option.active { border-color: var(--gold-dim); background: var(--gold-glow); }
    .size-name { font-size: 14px; font-weight: 500; color: var(--text); }
    .size-dim { font-size: 11px; color: var(--muted); letter-spacing: 0.04em; font-family: monospace; }

    /* Generate area */
    .generate-area { padding-top: 20px; display: flex; flex-direction: column; gap: 12px; }

    .processing-overlay {
      display: flex; align-items: center; gap: 14px; padding: 4px 0;
    }
    .processing-ring {
      width: 20px; height: 20px; flex-shrink: 0;
      border: 1.5px solid var(--border2); border-top-color: var(--gold);
      border-radius: 50%; animation: spin 1s linear infinite;
    }
    .processing-text { font-size: 13px; letter-spacing: 0.15em; text-transform: uppercase; color: var(--muted); }

    .error-bar {
      padding: 13px 16px; border: 1px solid rgba(217,91,91,0.3);
      background: rgba(217,91,91,0.06); border-radius: 9px;
      font-size: 14px; color: var(--error); font-weight: 400; line-height: 1.5;
    }

    .btn-generate {
      width: 100%; padding: 18px;
      background: linear-gradient(135deg, #C4A35A 0%, #9C7F3A 100%);
      color: var(--bg); border: none; border-radius: 10px;
      font-family: 'Syne', sans-serif; font-size: 14px; font-weight: 700;
      letter-spacing: 0.2em; text-transform: uppercase;
      cursor: pointer; transition: opacity 0.2s, transform 0.15s, box-shadow 0.2s;
      position: relative; overflow: hidden;
    }
    .btn-generate::before {
      content: ''; position: absolute; inset: 0;
      background: linear-gradient(135deg, rgba(255,255,255,0.14) 0%, transparent 55%);
    }
    .btn-generate:hover:not(:disabled) { opacity: 0.92; transform: translateY(-2px); box-shadow: 0 10px 28px rgba(196,163,90,0.3); }
    .btn-generate:active:not(:disabled) { transform: translateY(0); box-shadow: none; }
    .btn-generate:disabled { opacity: 0.28; cursor: not-allowed; transform: none; }

    /* ─── RESULT ─── */
    .result-section {
      max-width: 1160px; margin: 0 auto 0; padding: 48px 40px 0;
      animation: fadeUp 0.6s ease both;
    }
    .result-header { display: flex; align-items: center; gap: 24px; margin-bottom: 32px; }
    .result-title {
      font-family: 'Fraunces', serif;
      font-size: 30px; font-weight: 300; letter-spacing: 0.03em; color: var(--text);
      white-space: nowrap;
    }
    .result-title em { font-style: italic; color: var(--gold); }
    .result-line { flex: 1; height: 1px; background: linear-gradient(to right, var(--border2), transparent); }

    .result-card {
      background: var(--surface); border: 1px solid var(--border);
      border-radius: 14px; overflow: hidden;
      display: flex; align-items: stretch;
    }
    .result-img-wrap {
      flex: 1; background: var(--surface2);
      display: flex; align-items: center; justify-content: center;
      padding: 48px; min-height: 340px;
    }
    .result-img-wrap img {
      max-height: 340px; max-width: 100%; object-fit: contain;
      border-radius: 4px; box-shadow: 0 12px 40px rgba(0,0,0,0.6);
    }
    .result-side {
      width: 220px; border-left: 1px solid var(--border);
      padding: 32px 26px; display: flex; flex-direction: column; gap: 20px;
    }
    .result-meta { display: flex; flex-direction: column; gap: 6px; }
    .result-meta-label { font-size: 10px; letter-spacing: 0.25em; text-transform: uppercase; color: var(--muted); }
    .result-meta-value { font-size: 15px; color: var(--text); font-weight: 500; }
    .result-meta-dim { font-size: 12px; color: var(--gold-dim); font-family: monospace; letter-spacing: 0.06em; }

    .btn-dl {
      margin-top: auto; display: flex; align-items: center; justify-content: center; gap: 8px;
      padding: 13px; border: 1px solid var(--gold-dim); border-radius: 9px;
      background: transparent; color: var(--gold);
      font-family: 'Syne', sans-serif; font-size: 12px;
      letter-spacing: 0.18em; text-transform: uppercase;
      cursor: pointer; transition: background 0.2s, border-color 0.2s;
    }
    .btn-dl:hover { background: var(--gold-glow); border-color: var(--gold); }
    .btn-dl svg { width: 14px; height: 14px; stroke: currentColor; fill: none; stroke-width: 2; }

    /* ─── FOOTER ─── */
    footer {
      margin-top: 120px;
      border-top: 1px solid var(--border); padding: 44px 56px;
      display: grid; grid-template-columns: 1fr auto 1fr; align-items: center; gap: 24px;
    }
    .footer-logo {
      font-family: 'Fraunces', serif; font-size: 18px; font-weight: 300;
      letter-spacing: 0.22em; color: var(--gold-dim); text-transform: uppercase;
    }
    .footer-center {
      font-size: 13px; color: var(--muted); letter-spacing: 0.06em; text-align: center;
    }
    .footer-right {
      text-align: right; font-size: 12px; color: var(--muted); letter-spacing: 0.08em;
    }

    /* ─── UTILITIES ─── */
    .hidden { display: none !important; }

    .reveal {
      opacity: 0; transform: translateY(28px);
      transition: opacity 0.75s ease, transform 0.75s ease;
    }
    .reveal.visible { opacity: 1; transform: none; }
    .reveal-d1 { transition-delay: 0.08s; }
    .reveal-d2 { transition-delay: 0.18s; }
    .reveal-d3 { transition-delay: 0.28s; }
    .reveal-d4 { transition-delay: 0.38s; }

    @keyframes fadeUp { from { opacity: 0; transform: translateY(22px); } to { opacity: 1; transform: none; } }
    @keyframes spin { to { transform: rotate(360deg); } }
    @keyframes pulse { 0%, 100% { opacity: 0.6; transform: translate(-50%,-50%) scale(1); } 50% { opacity: 1; transform: translate(-50%,-50%) scale(1.08); } }
    @keyframes scrollPulse { 0%, 100% { opacity: 0.3; } 50% { opacity: 0.9; } }

    /* ─── RESPONSIVE ─── */
    @media (max-width: 960px) {
      nav { padding: 0 24px; }
      .nav-links { display: none; }
      .hero { padding: 100px 24px 60px; }
      .features { padding: 80px 24px; }
      .features-grid { grid-template-columns: 1fr; }
      .feature-card { border-radius: 12px !important; }
      .pricing { padding: 80px 24px; }
      .pricing-grid { grid-template-columns: 1fr; }
      .studio-header { padding: 0 24px; }
      .studio-wrap { padding: 0 20px; }
      .workspace { grid-template-columns: 1fr; }
      .upload-panel { border-right: 1px solid var(--border); border-bottom: none; border-radius: 18px 18px 0 0; }
      .settings-panel { border-radius: 0 0 18px 18px; }
      .how { padding: 80px 24px; }
      .how-steps { grid-template-columns: 1fr; gap: 48px; }
      .how-steps::before { display: none; }
      .result-section { padding: 40px 20px 0; }
      .result-card { flex-direction: column; }
      .result-side { width: 100%; border-left: none; border-top: 1px solid var(--border); }
      footer { grid-template-columns: 1fr; text-align: center; gap: 12px; padding: 32px 24px; }
      .footer-right { text-align: center; }
    }
  </style>
</head>
<body>

  <!-- ─── NAVIGATION ─── -->
  <nav id="mainNav">
    <a class="nav-logo" href="javascript:void(0)" onclick="window.scrollTo({top:0,behavior:'smooth'})">Visage</a>
    <ul class="nav-links">
      <li><a href="javascript:void(0)" onclick="document.getElementById('features').scrollIntoView({behavior:'smooth'})">产品功能</a></li>
      <li><a href="javascript:void(0)" onclick="document.getElementById('how').scrollIntoView({behavior:'smooth'})">使用流程</a></li>
      <li><a href="javascript:void(0)" onclick="document.getElementById('pricing').scrollIntoView({behavior:'smooth'})">套餐</a></li>
      <li><a href="javascript:void(0)" onclick="document.getElementById('studio').scrollIntoView({behavior:'smooth'})">生成工具</a></li>
    </ul>
    <a class="nav-cta" href="javascript:void(0)" onclick="document.getElementById('studio').scrollIntoView({behavior:'smooth'})">立即生成</a>
  </nav>

  <!-- ─── HERO ─── -->
  <section class="hero">
    <div class="hero-bg">
      <div class="hero-grid"></div>
      <div class="hero-spot"></div>
      <div class="hero-spot-2"></div>
    </div>

    <div class="hero-content">
      <div class="hero-eyebrow">AI Powered Portrait Studio</div>

      <h1>
        专业证件照<br>
        <em>即刻生成</em>
        <span class="line2">触手可及</span>
      </h1>

      <p class="hero-desc">
        上传您的照片，AI 精准分离人像，<br>
        自动匹配证件规格与底色，秒出高质量证件照。
      </p>

      <div class="hero-actions">
        <a class="btn-hero-primary" href="javascript:void(0)" onclick="document.getElementById('studio').scrollIntoView({behavior:'smooth'})">
          开始生成
          <svg viewBox="0 0 24 24"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </a>
        <a class="btn-hero-ghost" href="javascript:void(0)" onclick="document.getElementById('features').scrollIntoView({behavior:'smooth'})">了解功能</a>
      </div>
    </div>

    <div class="hero-scroll">
      <span class="scroll-label">滚动探索</span>
      <div class="scroll-line"></div>
    </div>
  </section>

  <!-- ─── FEATURES ─── -->
  <section class="features" id="features">
    <div class="features-header reveal">
      <div class="section-eyebrow">产品功能</div>
      <h2 class="section-title">为什么选择<br><em>VISAGE</em></h2>
      <p class="section-sub">从人像识别到规格输出，每一步都经过精心设计，让证件照生成变得简单而专业。</p>
    </div>

    <div class="features-grid">
      <div class="feature-card reveal reveal-d1">
        <div class="feature-num">01</div>
        <div class="feature-icon">
          <svg viewBox="0 0 24 24"><circle cx="12" cy="8" r="4"/><path d="M4 20c0-4 3.6-7 8-7s8 3 8 7"/><path d="M15 6.5c.8.8 1 2 .5 3"/></svg>
        </div>
        <div class="feature-name">精准人像分离</div>
        <div class="feature-desc">四款 AI 抠图模型可选，精确识别人体轮廓，发丝边缘自然过渡，无需手动修图。</div>
      </div>

      <div class="feature-card reveal reveal-d2">
        <div class="feature-num">02</div>
        <div class="feature-icon">
          <svg viewBox="0 0 24 24"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M3 9h18M9 21V9"/></svg>
        </div>
        <div class="feature-name">全类证件支持</div>
        <div class="feature-desc">覆盖身份证、护照、驾驶证、签证等 14 种规格，尺寸精准符合各类机构标准。</div>
      </div>

      <div class="feature-card reveal reveal-d3">
        <div class="feature-num">03</div>
        <div class="feature-icon">
          <svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="4"/><path d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M4.93 19.07l1.41-1.41M17.66 6.34l1.41-1.41"/></svg>
        </div>
        <div class="feature-name">智能底色匹配</div>
        <div class="feature-desc">选择证件类型，系统自动应用合规背景色，蓝底白底精准对应，告别手工调色。</div>
      </div>

      <div class="feature-card reveal reveal-d4">
        <div class="feature-num">04</div>
        <div class="feature-icon">
          <svg viewBox="0 0 24 24"><path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/></svg>
        </div>
        <div class="feature-name">处理速度迅速</div>
        <div class="feature-desc">高效推理引擎驱动，秒级完成人像分离与背景合成，秒级处理即可取图。</div>
      </div>
    </div>
  </section>

  <!-- ─── HOW IT WORKS ─── -->
  <section class="how" id="how">
    <div class="how-header reveal">
      <div class="section-eyebrow">使用流程</div>
      <h2 class="section-title">三步完成<br><em>专业证件照</em></h2>
      <p class="section-sub">从上传到下载，全程不超过 30 秒。</p>
    </div>

    <div class="how-steps">
      <div class="how-step reveal reveal-d1">
        <div class="how-step-num">1</div>
        <div class="how-step-title">上传照片</div>
        <div class="how-step-desc">点击或拖拽上传您的正面人像照片，支持 JPG / PNG 格式，建议使用清晰正面照。</div>
        <span class="how-step-tag">Upload</span>
      </div>
      <div class="how-step reveal reveal-d2">
        <div class="how-step-num">2</div>
        <div class="how-step-title">配置参数</div>
        <div class="how-step-desc">选择证件类型，系统自动匹配规格与底色。也可手动指定尺寸、背景色和抠图模型。</div>
        <span class="how-step-tag">Configure</span>
      </div>
      <div class="how-step reveal reveal-d3">
        <div class="how-step-num">3</div>
        <div class="how-step-title">生成下载</div>
        <div class="how-step-desc">点击生成，AI 秒级处理完成，预览满意后一键下载高质量证件照文件。</div>
        <span class="how-step-tag">Download</span>
      </div>
    </div>
  </section>

  <!-- ─── PRICING ─── -->
  <section class="pricing" id="pricing">
    <div class="pricing-header reveal">
      <div class="section-eyebrow">套餐</div>
      <h2 class="section-title">简单透明<br><em>的定价</em></h2>
      <p class="section-sub">当前所有功能完全免费，无次数限制，未来专业版持续扩展。</p>
    </div>

    <div class="pricing-grid">
      <!-- Free -->
      <div class="pricing-card featured reveal reveal-d1">
        <div class="pricing-badge">当前可用</div>
        <div class="pricing-plan">免费计划</div>
        <div class="pricing-price">¥0</div>
        <div class="pricing-period">永久免费 · 无次数限制</div>
        <div class="pricing-divider"></div>
        <ul class="pricing-features">
          <li>
            <svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg>
            AI 人像精准抠图
          </li>
          <li>
            <svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg>
            14 种证件照规格
          </li>
          <li>
            <svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg>
            4 款抠图模型可选
          </li>
          <li>
            <svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg>
            智能底色自动匹配
          </li>
          <li>
            <svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg>
            高质量 JPEG 输出
          </li>
          <li>
            <svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg>
            隐私安全保障
          </li>
        </ul>
        <a class="pricing-cta primary" href="javascript:void(0)" onclick="document.getElementById('studio').scrollIntoView({behavior:'smooth'})">立即免费使用</a>
      </div>

      <!-- Pro -->
      <div class="pricing-card reveal reveal-d2">
        <div class="pricing-badge soon">即将推出</div>
        <div class="pricing-plan">专业版</div>
        <div class="pricing-price" style="color:var(--muted);">敬请期待</div>
        <div class="pricing-period">更多强大功能 · 持续开发中</div>
        <div class="pricing-divider"></div>
        <ul class="pricing-features">
          <li class="dimmed">
            <svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg>
            所有免费功能
          </li>
          <li class="dimmed">
            <svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg>
            批量图片处理
          </li>
          <li class="dimmed">
            <svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg>
            多人同照检测
          </li>
          <li class="dimmed">
            <svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg>
            高清无损输出
          </li>
          <li class="dimmed">
            <svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg>
            美化增强算法
          </li>
          <li class="dimmed">
            <svg viewBox="0 0 24 24"><polyline points="20 6 9 17 4 12"/></svg>
            API 接口调用
          </li>
        </ul>
        <div class="pricing-cta ghost">即将推出</div>
      </div>
    </div>
  </section>

  <!-- ─── STUDIO / WORKSPACE ─── -->
  <section class="studio-section" id="studio">
    <div class="studio-header reveal">
      <div class="section-eyebrow">生成工具</div>
      <h2 class="section-title">VISAGE <em>Studio</em></h2>
      <p class="section-sub" style="margin:0 auto;">上传照片，配置参数，一键输出专业证件照。</p>
    </div>

    <div class="studio-wrap">
      <div class="workspace">

        <!-- Upload -->
        <div class="upload-panel">
          <div class="panel-label">上传照片</div>
          <div class="upload-zone" id="uploadZone">
            <input type="file" id="fileInput" accept="image/*" hidden >
            <div class="upload-hint" id="uploadHint">
              <div class="upload-icon-wrap">
                <svg viewBox="0 0 24 24"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" y1="3" x2="12" y2="15"/></svg>
              </div>
              <div>
                <p class="upload-hint-title">点击或拖拽上传</p>
                <p class="upload-hint-sub">JPG / PNG &nbsp;·&nbsp; 建议正面清晰人像</p>
              </div>
            </div>
            <img id="previewImg" class="preview-img hidden" alt="预览" >
            <div class="preview-replace hidden" id="previewReplace">更换照片</div>
          </div>
        </div>

        <!-- Settings -->
        <div class="settings-panel">

          <div class="setting-block">
            <div class="setting-title">背景颜色</div>
            <div class="color-swatches">
              <div class="swatch swatch-white active" data-color="#FFFFFF" style="background:#FFFFFF;" title="白色"></div>
              <div class="swatch" data-color="#438EF3" style="background:#438EF3;" title="蓝色"></div>
              <div class="swatch" data-color="#D43C3C" style="background:#D43C3C;" title="红色"></div>
              <div class="swatch swatch-custom" title="自定义颜色">
                <input type="color" id="customColor" value="#FFFFFF" >
                <span class="swatch-custom-icon">+</span>
              </div>
            </div>
          </div>

          <div class="setting-block">
            <div class="setting-title">抠图模型</div>
            <div class="model-list" id="modelList">
              <div class="model-option active" data-model="modnet_photographic_portrait_matting">
                <span class="model-name">MODNet 摄影版</span>
                <span class="model-desc">默认 · 人像稳定</span>
              </div>
              <div class="model-option" data-model="hivision_modnet">
                <span class="model-name">HivisionModNet</span>
                <span class="model-desc">定制 · 证件照优化</span>
              </div>
              <div class="model-option" data-model="birefnet-v1-lite">
                <span class="model-name">BiRefNet Lite</span>
                <span class="model-desc">新一代 · 边缘精细</span>
              </div>
              <div class="model-option" data-model="rmbg-1.4">
                <span class="model-name">RMBG 1.4</span>
                <span class="model-desc">专项 · 背景去除</span>
              </div>
            </div>
          </div>

          <div class="setting-block grow">
            <div class="setting-title">照片规格</div>
            <div class="size-list" id="sizeList">

              <div class="size-category">标准寸照</div>
              <div class="size-option active" data-value="413x295" data-label="一寸" data-color="#FFFFFF">
                <span class="size-name">一寸</span>
                <span class="size-dim">25×35 mm</span>
              </div>
              <div class="size-option" data-value="579x413" data-label="二寸" data-color="#FFFFFF">
                <span class="size-name">二寸</span>
                <span class="size-dim">35×49 mm</span>
              </div>
              <div class="size-option" data-value="531x378" data-label="小二寸" data-color="#FFFFFF">
                <span class="size-name">小二寸</span>
                <span class="size-dim">33×48 mm</span>
              </div>

              <div class="size-category">国内证件</div>
              <div class="size-option" data-value="378x307" data-label="居民身份证" data-color="#FFFFFF">
                <span class="size-name">居民身份证</span>
                <span class="size-dim">26×32 mm</span>
              </div>
              <div class="size-option" data-value="567x390" data-label="护照" data-color="#FFFFFF">
                <span class="size-name">护照</span>
                <span class="size-dim">33×48 mm</span>
              </div>
              <div class="size-option" data-value="378x260" data-label="驾驶证" data-color="#FFFFFF">
                <span class="size-name">驾驶证</span>
                <span class="size-dim">22×32 mm</span>
              </div>
              <div class="size-option" data-value="567x390" data-label="港澳通行证" data-color="#FFFFFF">
                <span class="size-name">港澳通行证</span>
                <span class="size-dim">33×48 mm</span>
              </div>
              <div class="size-option" data-value="441x358" data-label="社保/医保" data-color="#FFFFFF">
                <span class="size-name">社保 / 医保</span>
                <span class="size-dim">26×32 mm</span>
              </div>

              <div class="size-category">国际签证</div>
              <div class="size-option" data-value="600x600" data-label="美国签证" data-color="#FFFFFF">
                <span class="size-name">美国签证</span>
                <span class="size-dim">51×51 mm</span>
              </div>
              <div class="size-option" data-value="531x413" data-label="日本签证" data-color="#FFFFFF">
                <span class="size-name">日本签证</span>
                <span class="size-dim">35×45 mm</span>
              </div>
              <div class="size-option" data-value="531x413" data-label="申根签证" data-color="#FFFFFF">
                <span class="size-name">申根 / 欧洲签证</span>
                <span class="size-dim">35×45 mm</span>
              </div>
              <div class="size-option" data-value="531x413" data-label="英国签证" data-color="#FFFFFF">
                <span class="size-name">英国签证</span>
                <span class="size-dim">35×45 mm</span>
              </div>
              <div class="size-option" data-value="567x390" data-label="加拿大签证" data-color="#FFFFFF">
                <span class="size-name">加拿大签证</span>
                <span class="size-dim">35×45 mm</span>
              </div>

              <div class="size-category">考试报名类</div>
              <div class="size-option" data-value="579x413" data-label="考试报名照" data-color="#438EF3">
                <span class="size-name">考试报名照</span>
                <span class="size-dim">413 × 579 px</span>
              </div>

            </div>
          </div>

          <div class="generate-area">
            <div id="processingState" class="processing-overlay hidden">
              <div class="processing-ring"></div>
              <div class="processing-text">AI 处理中，请稍候…</div>
            </div>
            <div id="errorBar" class="error-bar hidden"></div>
            <button class="btn-generate" id="generateBtn" disabled>生成证件照</button>
          </div>

        </div>
      </div>

      <!-- Result -->
      <section class="result-section hidden" id="resultSection">
        <div class="result-header">
          <div class="result-title">生成<em>结果</em></div>
          <div class="result-line"></div>
        </div>
        <div class="result-card">
          <div class="result-img-wrap">
            <img id="resultImg" alt="证件照" >
          </div>
          <div class="result-side">
            <div class="result-meta">
              <div class="result-meta-label">类型</div>
              <div class="result-meta-value" id="metaLabel">—</div>
            </div>
            <div class="result-meta">
              <div class="result-meta-label">尺寸</div>
              <div class="result-meta-dim" id="metaDim">—</div>
            </div>
            <div class="result-meta">
              <div class="result-meta-label">背景</div>
              <div style="display:flex;align-items:center;gap:9px;margin-top:3px;">
                <div id="metaColorDot" style="width:16px;height:16px;border-radius:50%;border:1.5px solid var(--border2);flex-shrink:0;"></div>
                <div class="result-meta-dim" id="metaColor">—</div>
              </div>
            </div>
            <div class="result-meta">
              <div class="result-meta-label">模型</div>
              <div class="result-meta-value" id="metaModel" style="font-size:13px;">—</div>
            </div>
            <button class="btn-dl" id="downloadBtn">
              <svg viewBox="0 0 24 24"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
              下载证件照
            </button>
          </div>
        </div>
      </section>
    </div>
  </section>

  <!-- ─── FOOTER ─── -->
  <footer>
    <div class="footer-logo">Visage</div>
    <div class="footer-center">© 2026 VISAGE Studio &nbsp;·&nbsp; AI 智能证件照</div>
    <div class="footer-right">Made for <span style="color:var(--gold-dim);letter-spacing:0.04em;">COMP7530 IT Forum</span> &nbsp;·&nbsp; <span style="font-style:italic;color:var(--text2);">WU Feiyang</span></div>
  </footer>

  <script>
    const API_BASE = `http://${window.location.hostname || 'localhost'}:8000`;

    // Nav scroll effect
    const mainNav = document.getElementById("mainNav");
    window.addEventListener("scroll", () => {
      mainNav.classList.toggle("scrolled", window.scrollY > 40);
    }, { passive: true });

    // Scroll reveal
    const revealEls = document.querySelectorAll(".reveal");
    const revealObserver = new IntersectionObserver((entries) => {
      entries.forEach(e => { if (e.isIntersecting) { e.target.classList.add("visible"); revealObserver.unobserve(e.target); } });
    }, { threshold: 0.12 });
    revealEls.forEach(el => revealObserver.observe(el));

    // DOM refs
    const uploadZone     = document.getElementById("uploadZone");
    const fileInput      = document.getElementById("fileInput");
    const uploadHint     = document.getElementById("uploadHint");
    const previewImg     = document.getElementById("previewImg");
    const previewReplace = document.getElementById("previewReplace");
    const generateBtn    = document.getElementById("generateBtn");
    const processingState= document.getElementById("processingState");
    const errorBar       = document.getElementById("errorBar");
    const resultSection  = document.getElementById("resultSection");
    const resultImg      = document.getElementById("resultImg");
    const downloadBtn    = document.getElementById("downloadBtn");
    const customColor    = document.getElementById("customColor");
    const metaLabel      = document.getElementById("metaLabel");
    const metaDim        = document.getElementById("metaDim");
    const metaColor      = document.getElementById("metaColor");
    const metaColorDot   = document.getElementById("metaColorDot");
    const metaModel      = document.getElementById("metaModel");

    let selectedFile  = null;
    let selectedColor = "#FFFFFF";
    let selectedSize  = { value: "413x295", label: "一寸", dim: "25×35 mm" };
    let selectedModel = "modnet_photographic_portrait_matting";

    // Upload
    uploadZone.addEventListener("click", () => fileInput.click());
    previewReplace.addEventListener("click", (e) => { e.stopPropagation(); fileInput.click(); });
    uploadZone.addEventListener("dragover", (e) => { e.preventDefault(); uploadZone.classList.add("drag-over"); });
    uploadZone.addEventListener("dragleave", () => uploadZone.classList.remove("drag-over"));
    uploadZone.addEventListener("drop", (e) => {
      e.preventDefault(); uploadZone.classList.remove("drag-over");
      const file = e.dataTransfer.files[0];
      if (file && file.type.startsWith("image/")) handleFile(file);
    });
    fileInput.addEventListener("change", () => { if (fileInput.files[0]) handleFile(fileInput.files[0]); });

    function handleFile(file) {
      selectedFile = file;
      const reader = new FileReader();
      reader.onload = (e) => {
        previewImg.src = e.target.result;
        previewImg.classList.remove("hidden");
        previewReplace.classList.remove("hidden");
        uploadHint.classList.add("hidden");
        generateBtn.disabled = false;
      };
      reader.readAsDataURL(file);
      resultSection.classList.add("hidden");
      errorBar.classList.add("hidden");
    }

    // Color
    document.querySelectorAll(".swatch:not(.swatch-custom)").forEach((s) => {
      s.addEventListener("click", () => {
        document.querySelectorAll(".swatch").forEach((x) => x.classList.remove("active"));
        s.classList.add("active");
        selectedColor = s.dataset.color;
        customColor.value = selectedColor;
      });
    });
    customColor.addEventListener("input", () => {
      selectedColor = customColor.value;
      document.querySelectorAll(".swatch").forEach((x) => x.classList.remove("active"));
      customColor.closest(".swatch-custom").classList.add("active");
    });

    function applyColor(color) {
      selectedColor = color;
      customColor.value = color;
      document.querySelectorAll(".swatch").forEach((x) => x.classList.remove("active"));
      const match = [...document.querySelectorAll(".swatch[data-color]")]
        .find((s) => s.dataset.color.toUpperCase() === color.toUpperCase());
      if (match) {
        match.classList.add("active");
      } else {
        customColor.closest(".swatch-custom").classList.add("active");
      }
    }

    // Model
    const modelLabels = {
      "modnet_photographic_portrait_matting": "MODNet 摄影版",
      "hivision_modnet": "HivisionModNet",
      "birefnet-v1-lite": "BiRefNet Lite",
      "rmbg-1.4": "RMBG 1.4",
    };
    document.querySelectorAll(".model-option").forEach((opt) => {
      opt.addEventListener("click", () => {
        document.querySelectorAll(".model-option").forEach((x) => x.classList.remove("active"));
        opt.classList.add("active");
        selectedModel = opt.dataset.model;
      });
    });

    // Size
    document.querySelectorAll(".size-option").forEach((opt) => {
      opt.addEventListener("click", () => {
        document.querySelectorAll(".size-option").forEach((x) => x.classList.remove("active"));
        opt.classList.add("active");
        const dimEl = opt.querySelector(".size-dim");
        selectedSize = {
          value: opt.dataset.value,
          label: opt.dataset.label,
          dim: dimEl ? dimEl.textContent : opt.dataset.value,
        };
        if (opt.dataset.color) applyColor(opt.dataset.color);
      });
    });

    // Generate
    generateBtn.addEventListener("click", async () => {
      if (!selectedFile) return;

      const [height, width] = selectedSize.value.split("x").map(Number);
      const formData = new FormData();
      formData.append("image", selectedFile);
      formData.append("bg_color", selectedColor);
      formData.append("height", height);
      formData.append("width", width);
      formData.append("matting_model", selectedModel);

      generateBtn.disabled = true;
      processingState.classList.remove("hidden");
      errorBar.classList.add("hidden");
      resultSection.classList.add("hidden");

      try {
        const resp = await fetch(`${API_BASE}/api/generate`, { method: "POST", body: formData });
        if (!resp.ok) {
          const err = await resp.json();
          throw new Error(err.detail || `请求失败（${resp.status}）`);
        }
        const data = await resp.json();

        resultImg.src = `data:image/jpeg;base64,${data.image}`;
        metaLabel.textContent = selectedSize.label;
        metaDim.textContent   = `${width} × ${height} px`;
        metaColor.textContent  = selectedColor.toUpperCase();
        metaColorDot.style.background   = selectedColor;
        metaColorDot.style.borderColor  = selectedColor === "#FFFFFF" ? "var(--border2)" : selectedColor;
        metaModel.textContent  = modelLabels[selectedModel] || selectedModel;

        downloadBtn.onclick = () => downloadBase64(data.image, `visage-${selectedSize.label}.jpg`);

        resultSection.classList.remove("hidden");
        resultSection.scrollIntoView({ behavior: "smooth", block: "start" });
      } catch (e) {
        errorBar.textContent = e.message;
        errorBar.classList.remove("hidden");
      } finally {
        processingState.classList.add("hidden");
        generateBtn.disabled = false;
      }
    });

    function downloadBase64(b64, filename) {
      const byteStr = atob(b64);
      const ab = new ArrayBuffer(byteStr.length);
      const ia = new Uint8Array(ab);
      for (let i = 0; i < byteStr.length; i++) ia[i] = byteStr.charCodeAt(i);
      const blob = new Blob([ab], { type: "image/jpeg" });
      const url = URL.createObjectURL(blob);
      const a = document.createElement("a");
      a.href = url; a.download = filename; a.click();
      URL.revokeObjectURL(url);
    }
  </script>
</body>
</html>

